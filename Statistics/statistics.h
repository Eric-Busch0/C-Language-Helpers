#ifndef STATISTICS_H
#define STATISTICS_H
#include <stdlib.h>

float arr_average(float *arr, size_t arr_size);
float sum(float *arr, size_t arr_size);
float min(float *arr, size_t arr_size);
float max(float *arr, size_t arr_size);

#endif